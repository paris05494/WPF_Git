<Window x:Class="GitLogViewer.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GitLogViewer.Views"
        mc:Ignorable="d"
        WindowState="Maximized"
        Title="MainView" Height="450" Width="800">
    <StackPanel Margin="20" >
        <!--Dropdown path-->
        <TextBlock Text="Select Git Repository Path:" FontWeight="Bold" />
        <ComboBox ItemsSource="{Binding RepoPaths}"
                  SelectedItem="{Binding SelectedPath}" />

        <!--Load files button-->
        <Button Content="Load Files"
                Command="{Binding LoadFilesCommand}" Width="100" Margin="0,10,0,0"/>

        <!--all files in repo-->
        <TextBlock Text="Files in repository:" FontWeight="Bold" Margin="0,10,0,0"/>
        <ListBox ItemsSource="{Binding GitFiles}" Height="150"/>

        <!--ดู Git log-->
        <Button Content="Open Git Log Viewer"
                Command="{Binding OpenGitLogCommand}" Width="180" Margin="0,20,0,0"/>
    </StackPanel>
</Window>
