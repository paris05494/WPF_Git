<Window x:Class="GitLogViewer.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:GitLogViewer.Views"
        Title="MainView" WindowState="Maximized">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ดรอปดาวน์เลือก path -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Select Git Repository Path:" FontWeight="Bold" />
            <ComboBox ItemsSource="{Binding RepoPaths}" SelectedItem="{Binding SelectedPath}" />
        </StackPanel>

        <!-- InfoView -->
        <local:InfoView Grid.Row="1" DataContext="{Binding InfoVM}" />
    </Grid>
</Window>
