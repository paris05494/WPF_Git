<Window x:Class="GitLogViewer.Views.CopyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GitLogViewer.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="CopyView" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="เลือกรายชื่อ:" FontWeight="Bold" Margin="0,0,0,5"/>
            <DataGrid ItemsSource="{Binding People}" AutoGenerateColumns="False" Height="150">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Binding="{Binding IsSelected}" Header="เลือก" />
                    <DataGridTextColumn Binding="{Binding Person.Name}" Header="ชื่อ" />
                    <DataGridTextColumn Binding="{Binding Person.Lastname}" Header="นามสกุล" />
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock Text="เลือกปลายทาง:" FontWeight="Bold" Margin="0,10,0,5"/>
            <DataGrid ItemsSource="{Binding Targets}" AutoGenerateColumns="False" Height="150">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Binding="{Binding IsSelected}" Header="เลือก" />
                    <DataGridTextColumn Binding="{Binding FolderName}" Header="ชื่อโฟลเดอร์" />
                </DataGrid.Columns>
            </DataGrid>

            <Button Content="Submit" Command="{Binding SubmitCommand}" Margin="0,10,0,0" Width="100" HorizontalAlignment="Right"/>
        </StackPanel>
    </Grid>
</Window>
